[pytest]
# Keeping this file inside tests/ pins rootdir to tests/, so pytest does not
# build a Package node for the plugin root __init__.py (which cannot be
# imported outside the host source tree). pythonpath=. keeps `import conftest`
# working in the helper modules' file-based loader.
pythonpath = .
